const Logo = () => {
  return (
    <span className="direction-row flex gap-[4px] font-[Open_Sans] text-xl font-extrabold text-[var(--color-primary)] dark:text-[var(--color-primary-dark)]">
      NOTATKA
      <span className="rounded-lg bg-orange-400 px-[7px] text-[var(--color-primary)]">
        HUB
      </span>
    </span>
  );
};

export default Logo;
